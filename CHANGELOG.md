# Changelog

## v0.0.47

### 功能

- 新增启动默认菜单设置：设置页可选启动落页（发布工作台 / 项目发布），默认发布工作台

### 修复

- 修复预检报"无可发布模块"却无任何线索的问题：应用配置中的服务器被删除/重建后 id 失效（多选框回显为数字），聚合时被静默跳过。现在进入应用配置步骤即检测失效 id——项目内同名服务器唯一时自动修正并保留已配发布路径，否则提示重新选择；预检日志同步补全"未配置客户端路径 / 服务器不在勾选范围"等明确提示

### 其他

- 重写 README：功能改为文字描述并移除旧截图
- 项目指令文档统一迁移至 AGENTS.md（CLAUDE.md 改为导入链接）

## v0.0.46

### 功能

- 服务自发现导入重做：扫描本机/远端的服务按名称自动映射到应用配置各模块（WebApiHost/调度/WebClient/WpfClient/SpcMonitor），确认导入后直接在"应用配置"步骤生成服务条目（服务标识+发布路径），挂到被扫描的服务器下；不再把服务误存为服务器记录，无法识别模块的服务明确标注跳过

### 修复

- 修复发布工作台"应用配置"步骤不显示已选项目的问题（跳过向导第1步或表单缓存恢复时项目被覆盖为空）：项目以草稿为准强制对齐，跳过第1步时也允许在应用配置处直接选择项目（#1）
- 修复新手引导每次进入工作台反复弹出的问题：改为首次安装只自动弹出一次，直接关闭后不再弹；顶部"新手指引"手动入口不受影响

## v0.0.45

### 功能

- 新增"发布工作台"（置顶导航首位）：四步向导（选项目 → 选发布内容 → 应用配置 → 预检与发布），聚合既有链路一站式完成发布
- 新手引导：首次启动自动弹出全屏 5 步真实表单向导，Header 提供重播入口
- 服务自发现：SSH 连接可按 systemd/Docker/进程三链路自动发现已部署服务路径，配合设置中的发现前缀管理，一键认领纳管服务器

### 修复

- 修复发布工作台页面滚动失效的问题；预检/发布按钮移入表单卡片、未选代码源不再误报缺失、已存配置自动带出避免重复填写
- 修复服务器管理上传 SSH MCP 时因项目名/服务器名命名规则误拦截未纳管主机的问题；所属项目筛选下拉支持一键清空
- Directory Opus 打开目录改为新开标签页（NEWTAB=tofront），不再覆盖用户当前浏览的标签页，窗口后台/最小化时自动恢复置前

## v0.0.44

### 功能

- 无

### 修复

- 修复 MCP 服务 list_tools 缺少 ttl_ms/cache_scope 参数，被严格协议校验的客户端整体拒绝工具列表的问题：手写 list_tools 返回 ttl_ms=0（不缓存）、cache_scope=Private，符合动态数据语义
- 修复应用配置 WpfClient 选项卡首次勾选服务器不显示发布路径输入项的问题：旧单服务器数据（serverId 为 null）归一化时补齐 serverIds/serverArr 字段，避免切换监听访问 serverArr.length 报错中断

## v0.0.43

### 功能

- WpfClient 发布支持同时发布到多台不同服务器（与 WebApiHost/WebClient/ScheduleServer/SpcMonitor 一致），覆盖应用配置多选、主页一键/手动发布、生成发布、文件手动发布、发布前备份、备份还原与导入导出

## v0.0.42

### 功能

- 新增独立"关于"页面（菜单位于设置下方）：显示当前版本号，提供手动"检查更新"入口（与启动时自动检查更新复用同一升级弹窗与下载安装流程）；v0.0.41 内嵌在设置页中的关于区块同步移除

## v0.0.41

### 功能

- 设置页新增"关于"区块：显示当前版本号，提供手动"检查更新"入口（与启动时自动检查更新复用同一升级弹窗与下载安装流程）

### 修复

- 修复窗口标题栏不再显示版本号的问题：版本号重构后误用 document.title 设置（不会同步 Tauri 原生窗口标题），改为 Rust 侧启动时拼接版本，恢复"SMOM平台发布工具 vX.Y.Z"格式

## v0.0.40

### 功能

- 升级弹窗显示内容改为当前版本的 GitHub Release 说明（实时拉取 Release 正文渲染），拉取失败（限流/Release 未创建/断网）自动回退 update.json notes；弹窗内容区加最大高度滚动

## v0.0.39

### 修复

- 修复服务启停/本地命令/文件复制重试设置静默失效的 Bug：getRetryArgs 参数键由 snake_case 改为 camelCase 以匹配 Tauri 命令取参规则；此前无论设置页配置多少，实际恒按默认值执行（服务启停 3 次/2 秒、文件复制单次）

## v0.0.38

### 修复

- 服务重试设置命名由"服务关闭重试"改为"服务启停重试"（win_service_stop_retry → win_service_retry），数据库列名升级时自动迁移、缺列时自动补建，老数据完整保留
- 远程恢复对话框服务启停命令未使用设置中配置的重试次数/间隔，现与其他发布入口一致

## v0.0.37

### 功能

- 设置菜单新增开机自启开关，开启后开机自动启动应用并驻留系统托盘

### 修复

- DLL名称模式生成发布日志时未清理旧日志，现与 TFS/Git 一致，仅保留最新日志
- CI 移除 auto-tag job（GITHUB_TOKEN 触发 workflow_dispatch 受 GitHub 策略限制），发布改为 tag 推送触发，消除每次 push 的 403 报错

## v0.0.35

### 修复

- DLL名称获取方式复制前清空目标目录，防止旧 DLL 残留混入发布包

## v0.0.34

### 功能

- TFS/Git 查询记录与生成日志的日期选择默认 00:00:00 ~ 23:59:59
- 应用配置 TFS/Git 日期选择默认时间
